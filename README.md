# adhan-menu-native — update feed

Public update feed for the native macOS **Adhan** menu-bar app.
`latest.json` is polled by the app (once/day and via ••• → Check for Updates).
Release binaries are attached to this repo's Releases. Source lives in the private `adhan-menu-native`.
